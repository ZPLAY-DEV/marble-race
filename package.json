{
  "name": "marble-race",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "verify": "node scripts/verify.mjs",
    "soak": "node scripts/soak.mjs",
    "shots": "node scripts/shots.mjs",
    "seedcheck": "node scripts/seedcheck.mjs"
  },
  "dependencies": {
    "playcanvas": "2.21.3"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "playwright": "^1.62.1",
    "typescript": "^5.6.0",
    "vite": "^7.0.0",
    "vitest": "^3.0.0"
  }
}
